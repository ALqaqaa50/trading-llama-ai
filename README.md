# Trading Llama AI 🦙📈

نظام تداول ذكي متكامل مدعوم بالذكاء الاصطناعي (Llama) لتحليل أسواق العملات الرقمية وتوليد إشارات تداول احترافية.

## 🌟 المميزات الرئيسية

### 🤖 ذكاء اصطناعي متقدم
- **نموذج Llama المتخصص**: تحليل ذكي للأسواق مع فهم عميق للسياق
- **توصيات مبنية على البيانات**: تقييم شامل مع نسبة ثقة لكل توصية
- **واجهة محادثة تفاعلية**: اسأل أي سؤال عن التداول واحصل على إجابات احترافية

### 📊 تحليل فني شامل
- **المؤشرات الفنية**: RSI, MACD, Bollinger Bands, SMA, EMA, ATR, Stochastic
- **أنماط الشموع اليابانية**: 10+ نمط احترافي (Hammer, Doji, Engulfing, Morning/Evening Star، وغيرها)
- **كشف الاتجاه التلقائي**: تحديد الاتجاهات الصاعدة والهابطة والجانبية

### ⚠️ إدارة مخاطر متقدمة
- **Position Sizing**: حساب حجم الصفقة المثالي بناءً على رأس المال والمخاطرة
- **Stop Loss & Take Profit ديناميكي**: مبني على ATR ونسبة المخاطرة إلى العائد
- **R-Multiples (نظام Van Tharp)**: تتبع أداء الصفقات بطريقة احترافية
- **Kelly Criterion**: حساب حجم الصفقة الأمثل
- **Sharpe Ratio & Max Drawdown**: مقاييس أداء شاملة

### 🔐 أمان متقدم
- **تشفير AES**: حماية مفاتيح API بتشفير قوي
- **تخزين آمن**: مفاتيح API محفوظة بشكل دائم ومشفر في قاعدة البيانات
- **مصادقة المستخدم**: نظام مصادقة متكامل مع Manus OAuth

### 📡 اتصال مباشر بـ OKX
- **بيانات فورية**: أسعار حية، حجم التداول، دفتر الأوامر
- **بيانات تاريخية**: OHLCV للتحليل والاختبار الخلفي
- **معلومات الحساب**: الرصيد والصفقات المفتوحة

## 🏗️ البنية المعمارية

```
trading_llama_ai/
├── client/                    # الواجهة الأمامية (React + TypeScript)
│   ├── src/
│   │   ├── pages/            # صفحات التطبيق
│   │   │   ├── Home.tsx      # الصفحة الرئيسية
│   │   │   ├── ApiKeySetup.tsx    # إعداد مفاتيح OKX
│   │   │   └── TradingDashboard.tsx  # لوحة التداول الرئيسية
│   │   ├── components/       # مكونات واجهة المستخدم
│   │   └── lib/trpc.ts       # عميل tRPC
│   └── public/               # الملفات الثابتة
│
├── server/                    # الخادم الخلفي (Express + tRPC)
│   ├── analysis/             # وحدات التحليل الفني
│   │   ├── indicators.ts     # المؤشرات الفنية
│   │   ├── candlestickPatterns.ts  # أنماط الشموع اليابانية
│   │   └── riskManagement.ts      # إدارة المخاطر
│   ├── services/             # الخدمات
│   │   ├── okxService.ts     # الاتصال بـ OKX
│   │   └── aiTradingAssistant.ts  # مساعد التداول الذكي
│   ├── utils/                # أدوات مساعدة
│   │   └── encryption.ts     # تشفير المفاتيح
│   ├── db.ts                 # دوال قاعدة البيانات
│   └── routers.ts            # نقاط نهاية API
│
├── drizzle/                   # قاعدة البيانات
│   └── schema.ts             # مخطط الجداول
│
└── todo.md                    # خريطة طريق المشروع
```

## 🗄️ قاعدة البيانات

النظام يستخدم 8 جداول متخصصة:

| الجدول | الوصف |
|--------|-------|
| `users` | معلومات المستخدمين والمصادقة |
| `api_keys` | مفاتيح OKX المشفرة |
| `market_data` | بيانات الأسعار التاريخية (OHLCV) |
| `trading_signals` | إشارات التداول المولدة |
| `trades` | سجل الصفقات المنفذة |
| `backtest_results` | نتائج الاختبار الخلفي |
| `chat_messages` | سجل المحادثات مع AI |
| `indicator_cache` | ذاكرة مؤقتة للمؤشرات |

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js 22+
- pnpm
- قاعدة بيانات MySQL/TiDB

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/your-username/trading_llama_ai.git
cd trading_llama_ai
```

2. **تثبيت التبعيات**
```bash
pnpm install
```

3. **إعداد قاعدة البيانات**
```bash
pnpm db:push
```

4. **تشغيل المشروع**
```bash
pnpm dev
```

5. **فتح المتصفح**
```
http://localhost:3000
```

## 📖 دليل الاستخدام

### 1. إعداد مفاتيح OKX

1. سجل الدخول إلى التطبيق
2. ستظهر لك صفحة إعداد المفاتيح تلقائياً
3. أدخل:
   - **API Key**: مفتاح API من OKX
   - **Secret Key**: المفتاح السري
   - **Passphrase**: كلمة المرور
4. اختبر الاتصال ثم احفظ المفاتيح

> 💡 **كيفية الحصول على مفاتيح OKX:**
> 1. سجل الدخول إلى [OKX](https://www.okx.com)
> 2. اذهب إلى **Account** → **API Management**
> 3. أنشئ API Key جديد مع صلاحيات القراءة (Read)
> 4. احفظ المفاتيح في مكان آمن

### 2. لوحة التداول

بعد إعداد المفاتيح، ستنتقل تلقائياً إلى لوحة التداول حيث يمكنك:

- **مراقبة السوق**: عرض السعر الحالي، أعلى/أدنى سعر، الحجم
- **الرسوم البيانية**: تصور بيانات الأسعار (قريباً)
- **المحادثة مع AI**: اطرح أسئلة واحصل على تحليلات
- **عرض الرصيد**: متابعة رصيدك في المنصة

### 3. التحليل الذكي

استخدم زر "تحليل السوق" للحصول على:
- تقييم شامل للوضع الحالي
- توصية (شراء/بيع/انتظار)
- نسبة الثقة في التوصية
- نقاط رئيسية تدعم القرار

## 🔧 API Reference

### نقاط النهاية الرئيسية

#### إدارة المفاتيح
```typescript
// حفظ مفتاح API جديد
trpc.apiKeys.save.useMutation({
  exchange: 'okx',
  apiKey: string,
  secretKey: string,
  passphrase: string,
})

// جلب قائمة المفاتيح
trpc.apiKeys.list.useQuery()

// اختبار الاتصال
trpc.apiKeys.testConnection.useMutation({ exchange: 'okx' })
```

#### بيانات السوق
```typescript
// جلب السعر الحالي
trpc.market.getTicker.useQuery({ symbol: 'BTC/USDT' })

// جلب بيانات OHLCV
trpc.market.fetchOHLCV.useQuery({
  symbol: 'BTC/USDT',
  timeframe: '1h',
  limit: 100,
})

// جلب الرصيد
trpc.market.getBalance.useQuery()
```

#### الذكاء الاصطناعي
```typescript
// تحليل السوق
trpc.ai.analyzeMarket.useMutation({ symbol: 'BTC/USDT' })

// محادثة مع AI
trpc.ai.chat.useMutation({
  message: 'ما رأيك في BTC الآن؟',
  symbol: 'BTC/USDT',
})

// جلب سجل المحادثات
trpc.ai.getChatHistory.useQuery()
```

## 📚 المفاهيم الأساسية

### R-Multiples (نظام Van Tharp)

**R** هو مقدار المخاطرة الأولية في الصفقة. إذا كان:
- R = 1: ربحت بالضبط ما خاطرت به
- R = 2: ربحت ضعف ما خاطرت به
- R = -1: خسرت كامل المخاطرة

**مثال:**
```
رأس المال: $10,000
نسبة المخاطرة: 2%
المخاطرة: $200 (1R)

سعر الدخول: $50,000
Stop Loss: $49,000
الفرق: $1,000

حجم الصفقة: $200 / $1,000 = 0.2 BTC

إذا بعت عند $52,000:
الربح = ($52,000 - $50,000) × 0.2 = $400
R-Multiple = $400 / $200 = 2R
```

### Position Sizing

حساب حجم الصفقة المثالي:

```typescript
const positionSize = calculatePositionSize({
  accountBalance: 10000,
  riskPercentage: 2,
  entryPrice: 50000,
  stopLossPrice: 49000,
});

// النتيجة:
// quantity: 0.2 BTC
// riskAmount: $200
// positionValue: $10,000
```

### أنماط الشموع اليابانية

| النمط | النوع | الوصف |
|-------|------|-------|
| **Hammer** | صعودي | جسم صغير في الأعلى، ذيل طويل في الأسفل |
| **Shooting Star** | هبوطي | جسم صغير في الأسفل، ذيل طويل في الأعلى |
| **Engulfing** | انعكاس | شمعة تبتلع الشمعة السابقة بالكامل |
| **Morning Star** | صعودي | 3 شموع تشير لانعكاس صعودي |
| **Evening Star** | هبوطي | 3 شموع تشير لانعكاس هبوطي |
| **Doji** | محايد | جسم صغير جداً، تردد في السوق |

## 🛡️ الأمان

### تشفير المفاتيح

المفاتيح محمية بـ AES-256-CBC:

```typescript
// التشفير
const encrypted = encryptApiKeys({
  apiKey: 'your-api-key',
  secretKey: 'your-secret-key',
  passphrase: 'your-passphrase',
});

// فك التشفير (تلقائي عند الاستخدام)
const decrypted = decryptApiKeys(encrypted);
```

### أفضل الممارسات

1. **لا تشارك مفاتيح API أبداً**
2. **استخدم صلاحيات القراءة فقط** إذا لم تكن بحاجة للتداول
3. **فعّل IP Whitelist** في إعدادات OKX
4. **راجع الصفقات** قبل التنفيذ
5. **احتفظ بنسخة احتياطية** من المفاتيح في مكان آمن

## 🧪 الاختبار

### اختبار المؤشرات الفنية

```typescript
import { calculateRSI, calculateMACD } from './server/analysis/indicators';

const prices = [100, 102, 101, 105, 107, 106, 108];
const rsi = calculateRSI(prices, 14);
const macd = calculateMACD(prices);

console.log('RSI:', rsi[rsi.length - 1]);
console.log('MACD:', macd.macd[macd.macd.length - 1]);
```

### اختبار أنماط الشموع

```typescript
import { analyzePatterns } from './server/analysis/candlestickPatterns';

const candles = [
  { timestamp: 1, open: 100, high: 105, low: 99, close: 103, volume: 1000 },
  { timestamp: 2, open: 103, high: 106, low: 102, close: 104, volume: 1200 },
];

const patterns = analyzePatterns(candles, 'uptrend');
console.log('Patterns found:', patterns);
```

## 🤝 المساهمة

نرحب بالمساهمات! إذا كنت تريد المساهمة:

1. Fork المشروع
2. أنشئ فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى الفرع (`git push origin feature/amazing-feature`)
5. افتح Pull Request

## 📝 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## ⚠️ تنويه

**هذا النظام للأغراض التعليمية والبحثية فقط.** التداول في الأسواق المالية ينطوي على مخاطر عالية. استخدم هذا النظام على مسؤوليتك الخاصة. نحن لا نتحمل أي مسؤولية عن الخسائر المالية.

## 📞 الدعم

- **Issues**: [GitHub Issues](https://github.com/your-username/trading_llama_ai/issues)
- **Discussions**: [GitHub Discussions](https://github.com/your-username/trading_llama_ai/discussions)
- **Email**: support@tradingllama.ai

## 🙏 شكر وتقدير

- **Steve Nison** - لكتابه الرائد عن الشموع اليابانية
- **Van Tharp** - لنظام R-Multiples وإدارة المخاطر
- **OKX** - لتوفير API قوي ومستقر
- **Llama** - لنموذج الذكاء الاصطناعي المفتوح المصدر

---

**صُنع بـ ❤️ بواسطة Manus AI**

⭐ إذا أعجبك المشروع، لا تنسى إعطائه نجمة على GitHub!
